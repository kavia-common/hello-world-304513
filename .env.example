# Optional / future configuration (NOT required for current mock implementation)

# Base URL for a future external weather provider API (e.g., https://api.openweathermap.org/data/2.5)
WEATHER_API_BASE_URL=

# API key for the future external weather provider
WEATHER_API_KEY=
